<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>StartSite — Luxury · Trusted Digital Experiences</title>

  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&family=Playfair+Display:wght@400;600;700&display=swap" rel="stylesheet">

  <meta name="description" content="StartSite — premium web experiences combining luxury design with enterprise-grade engineering. Clear onboarding for new clients." />

  <style>
    /* ====== THEME VARIABLES ====== */
    :root{
      --bg: #070707;
      --panel: #0e0e10;
      --silver: #d6d6d6;
      --muted: #9a9a9a;
      --gold: #bf9538;
      --gold-2: #e7c17b;
      --glass: rgba(255,255,255,0.03);
      --accent: linear-gradient(90deg,var(--gold),var(--gold-2));
      --radius: 12px;
      --max-width: 1200px;
      --ease: cubic-bezier(.2,.9,.2,1);
      --focus-ring: 3px;
    }

    /* BASE */
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      font-family:Inter, system-ui, -apple-system, "Segoe UI", Roboto, Arial;
      font-size:16px;
      color:var(--silver);
      background:
        radial-gradient(circle at 10% 10%, rgba(255,255,255,0.02), transparent 8%),
        linear-gradient(180deg, rgba(255,255,255,0.01), transparent 30%),
        var(--bg);
      line-height:1.5;
    }
    a{color:inherit;text-decoration:none}

    .container{
      max-width:var(--max-width);
      margin:0 auto;
      padding:36px 20px;
    }

    .skip-link{
      left:8px;
      top:8px;
      transform:translateY(-120%);
      position:fixed;
      background:#fff;color:#000;padding:8px 10px;border-radius:6px;z-index:9999;
    }
    .skip-link:focus{ transform:none; outline:none }

    /* HEADER */
    header{ position:sticky; top:14px; z-index:60; backdrop-filter: blur(6px); margin-bottom:18px; }
    .header-inner{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:20px;
      padding:10px 0;
    }

    nav{ display:flex; gap:14px; align-items:center; }
    nav a{
      padding:8px 12px;
      border-radius:10px;
      font-weight:600;
      font-size:14px;
      transition: all 180ms var(--ease)
    }
    nav a:hover{
      color:#0b0b0b;
      background:var(--gold-2);
      transform:translateY(-3px)
    }
    .cta{
      padding:10px 16px;
      border-radius:12px;
      background:var(--accent);
      color:#0b0b0b;
      font-weight:700;
    }

    /* HERO */
    .hero{
      display:grid;
      grid-template-columns: 1fr 420px;
      gap:28px;
      align-items:center;
      margin:6px 0 36px;
    }

    .hero-card{
      background: rgba(255,255,255,0.015);
      border-radius: var(--radius);
      padding:36px;
      border:1px solid rgba(255,255,255,0.03);
      box-shadow: 0 30px 80px rgba(0,0,0,0.65);
    }

    .kicker{
      display:inline-block;
      font-weight:700;
      font-size:13px;
      color:var(--gold);
      background:rgba(191,149,56,0.06);
      padding:6px 10px;
      border-radius:999px;
      margin-bottom:12px
    }

    h1{
      margin:6px 0 14px;
      font-family:'Playfair Display',serif;
      font-size:36px;
      color:#fff;
      line-height:1.05
    }

    .lead{ color:var(--silver); font-size:16px; max-width:58ch; margin-bottom:18px }

    .btn{
      padding:12px 16px;
      border-radius:10px;
      border:1px solid rgba(255,255,255,0.04);
      font-weight:700;
      cursor:pointer;
      background:transparent;
      color:var(--silver);
    }
    .btn.primary{
      background:var(--accent);
      color:#0b0b0b;
    }
    .btn.secondary{ border:1px solid rgba(255,255,255,0.06) }

    /* SECTION HEADINGS */
    section{ margin-bottom:28px; }
    .section-head{ display:flex; align-items:center; justify-content:space-between; gap:12px; margin-bottom:14px }
    .section-head h2{ margin:0; font-family:'Playfair Display',serif; color:#fff }

    /* STEPS */
    .steps{ display:flex; gap:14px; flex-wrap:wrap }
    .step{
      flex:1; min-width:200px;
      background:rgba(255,255,255,0.008);
      padding:16px;
      border-radius:10px;
      border:1px solid rgba(255,255,255,0.03)
    }
    .step .num{
      background:var(--accent);
      color:#080808;
      font-weight:800;
      width:42px;height:42px;border-radius:9px;
      display:grid;place-items:center;
      margin-bottom:10px
    }

    /* CONTACT FORM */
    .contact-form {
      display:flex;
      flex-direction:column;
      gap:14px;
      width:100%;
      background: rgba(255,255,255,0.01);
      padding:22px;
      border-radius:12px;
      border:1px solid rgba(255,255,255,0.03);
      box-shadow: 0 18px 40px rgba(0,0,0,0.6);
    }
    .contact-form input,
    .contact-form textarea {
      padding:12px 14px;
      border-radius:10px;
      border:1px solid rgba(255,255,255,0.06);
      background:transparent;
      color:var(--silver);
      font-size:15px;
    }
    .contact-btn {
      padding:12px 16px;
      border-radius:10px;
      border:none;
      background: var(--accent);
      color:#080808;
      font-weight:800;
      font-size:16px;
      cursor:pointer;
    }

    footer{
      border-top:1px solid rgba(255,255,255,0.03);
      padding:26px 0;
      color:var(--muted);
      font-size:14px
    }

    /* SCROLL REVEAL */
    .reveal{ opacity:0; transform:translateY(14px); transition: 520ms var(--ease) }
    .reveal.visible{ opacity:1; transform:none }

  </style>
</head>
<body>

  <a class="skip-link" href="#main">Skip to content</a>

  <div class="container" id="page">

    <!-- HEADER -->
    <header>
      <div class="header-inner reveal">
        <div class="brand">
          <div class="logo"></div>
          <div>
            <div class="name" style="color:var(--gold);font-weight:700;">StartSite</div>
          </div>
        </div>

        <nav>
          <a href="#services">Services</a>
          <a href="#innovation">Innovation</a>
          <a href="#how">How it works</a>
          <a href="#faq">FAQ</a>
          <a class="cta" href="#contact">Get a Consultation</a>
        </nav>
      </div>
    </header>

    <!-- MAIN -->
    <main id="main" class="reveal">

      <!-- HERO -->
      <section class="hero">
        <div class="hero-card">
          <div class="kicker">Start with confidence</div>
          <h1>Premium websites that feel effortless</h1>
          <p class="lead">We design and build elegant, secure, and performant digital experiences.</p>

          <div style="display:flex;gap:12px;">
            <button class="btn primary" onclick="scrollToSection('contact')">Request a Consultation</button>
            <button class="btn secondary" onclick="scrollToSection('how')">How it works — 3 steps</button>
          </div>
        </div>

        <aside class="feature-panel"></aside>
      </section>

      <!-- SERVICES -->
      <section id="services" class="reveal">
        <div class="section-head">
          <div>
            <div class="kicker">Services</div>
            <h2>What we offer</h2>
          </div>
        </div>
      </section>

      <!-- INNOVATION -->
      <section id="innovation" class="reveal">
        <div class="section-head">
          <div>
            <div class="kicker">Innovation</div>
            <h2>Modern tech</h2>
          </div>
        </div>
      </section>

      <!-- HOW IT WORKS -->
      <section id="how" class="reveal">
        <div class="section-head">
          <div>
            <div class="kicker">Getting started</div>
            <h2>How StartSite works — three simple steps</h2>
          </div>
        </div>

        <div class="steps">
          <div class="step">
            <div class="num">1</div>
            Intro & Goals
          </div>
          <div class="step">
            <div class="num">2</div>
            Design & Approval
          </div>
          <div class="step">
            <div class="num">3</div>
            Launch & Handover
          </div>
        </div>
      </section>

      <!-- FAQ -->
      <section id="faq" class="reveal">
        <div class="section-head">
          <div>
            <div class="kicker">FAQ</div>
            <h2>Common questions</h2>
          </div>
        </div>
      </section>

      <!-- CONTACT -->
      <section id="contact" class="reveal">
        <div class="section-head">
          <div>
            <div class="kicker">Contact</div>
            <h2>Ready to get started?</h2>
          </div>
        </div>

        <form class="contact-form" action="https://api.web3forms.com/submit" method="POST">
          <input type="hidden" name="access_key" value="0fad912e-e1e4-4917-8d47-6e0a5cb18887">

          <input type="email" name="email" placeholder="Email" required>
          <input type="text" name="name" placeholder="Name" required>
          <textarea name="message" placeholder="Tell us how we can help..." required></textarea>

          <button class="contact-btn">Request Consultation</button>
        </form>
      </section>
    </main>

    <!-- FOOTER -->
    <footer class="reveal">
      © <span id="year"></span> StartSite
    </footer>
  </div>

  <script>
    document.getElementById('year').textContent = new Date().getFullYear();

    /* FIXED: SMOOTH SCROLL FUNCTION */
    function scrollToSection(id){
      const el = document.getElementById(id);
      if (!el) return;
      el.scrollIntoView({ behavior: "smooth", block: "start" });
    }

    /* Reveal */
    const revealEls = document.querySelectorAll('.reveal');
    const obs = new IntersectionObserver(entries => {
      entries.forEach(e => {
        if (e.isIntersecting) {
          e.target.classList.add('visible');
          obs.unobserve(e.target);
        }
      })
    }, {threshold:0.12});
    revealEls.forEach(el => obs.observe(el));
  </script>

</body>
</html>
